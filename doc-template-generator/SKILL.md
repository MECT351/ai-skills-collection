---
name: 文档模板生成器
description: 输入文档类型和需求，自动生成结构化文档模板。报告/方案/计划/总结，4大场景覆盖办公文档全需求。纯Python标准库，输出标准Markdown格式。
version: "1.0.0"
updated_at: "2026-06-03"
category: ["办公工具", "文档写作"]
tags: ["文档模板", "报告模板", "方案模板", "Markdown", "文档生成"]
trigger: ["生成文档模板", "报告模板", "方案模板", "工作计划", "文档结构"]
---

# 文档模板生成器

> 输入文档类型，输出结构化模板。不是阅读文档，是文档骨架生成工具。

## 核心能力

1. **报告模板生成** — 输入报告类型 → 输出完整报告骨架
2. **方案模板生成** — 输入方案主题 → 输出方案文档结构
3. **计划模板生成** — 输入计划目标 → 输出工作计划模板
4. **总结模板生成** — 输入总结范围 → 输出总结文档模板

---

## 快速开始

### 流程1：生成报告模板

```bash
python scripts/report.py --input '{"type": "周报", "department": "技术部", "period": "2026年第23周"}'
```

支持类型：日报/周报/月报/季报/年报/专项报告

### 流程2：生成方案模板

```bash
python scripts/proposal.py --input '{"topic": "AI智能体商业化", "target": "投资人", "scope": "技术方案+商业模式"}'
```

### 流程3：生成计划模板

```bash
python scripts/plan.py --input '{"goal": "Q3产品上线", "duration": "3个月", "team_size": 5}'`
```

### 流程4：生成总结模板

```bash
python scripts/summary.py --input '{"scope": "年度总结", "role": "技术负责人", "highlights": ["项目A上线", "团队扩展"]}'
```

---

## 设计原则

1. 输出标准Markdown — 任何编辑器都能打开
2. 模板包含占位提示 — 知道每处该填什么
3. 纯标准库 — 零安装
4. JSON标准化 — Agent好对接
